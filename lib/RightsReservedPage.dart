import 'package:flutter/material.dart';

class RightsReservedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Rights Reserved')),
      body: Center(child: Text('This is the Rights Reserved page.')),
    );
  }
}